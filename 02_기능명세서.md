# 기능명세서 — AI 디지털 약자 내비게이터

> **작업용 문서 세트 (2/5)**
> 우선순위 3단계: **[핵심]** 없으면 데모 불가 · **[선택]** 시간 남으면 · **[제외]** 이번엔 안 함

---

## 우선순위 한눈에 보기

| ID | 기능 | 우선순위 | 비고 |
|---|---|---|---|
| F1 | 화면 읽기 (접근성 서비스) | **핵심** | AI의 '눈' |
| F2 | 목표 이해 / 입력 | **핵심** | 초기엔 텍스트·버튼 |
| F3 | 다음 단계 판단 (AI) | **핵심** | 서비스의 '두뇌', 참신성 핵심 |
| F4 | 캐릭터 안내원 표시·이동 | **핵심** | AI의 '손', 데모 임팩트 |
| F5 | 음성 안내 (TTS) | **핵심** | 디지털 약자 타깃상 필수 |
| F6 | 화면 변화 감지 후 재판단 루프 | **핵심** | 챗봇과 내비를 가르는 지점 |
| F7 | 캐릭터 표정·반응 동작 | 선택 | UX 온기 |
| F8 | 캐릭터가 목표 버튼 안 가리게 배치 | 선택 | 은근히 중요 |
| F9 | 잘못 눌렀을 때 복구 안내 | 선택 | 실서비스엔 중요, 데모엔 정해진 경로만 |
| F10 | 음성으로 목표 입력 (STT) | 선택 | 타이핑 어려운 사용자용 |
| F11 | 여러 앱 지원 | 선택 | 데모는 하나만, 확장성은 발표로 |
| F12 | 글자 확대 / 고대비 모드 | 선택 | 인상 점수용 |
| F13 | 캐릭터 선택 | 선택 | 부가 |
| X1 | 대신 눌러주는 자동 실행 | **제외** | 자립 코칭 차별점과 배치 |
| X2 | 로그인 / 회원가입 | **제외** | 데모 불필요 |
| X3 | 사용 기록 저장 | **제외** | 데모 불필요 |
| X4 | iOS 지원 | **제외** | OS 제약, 로드맵 |
| X5 | 다국어 | **제외** | 데모 불필요 |

---

## 핵심 기능 상세

### F1. 화면 읽기 (접근성 서비스)
- **설명:** 안드로이드 접근성 서비스로 현재 화면에 표시된 UI 요소(버튼·텍스트·입력창)의 **텍스트, 화면 좌표, 요소 유형**을 구조화된 데이터로 수집한다.
- **입력:** 현재 활성 화면 (다른 앱 포함)
- **출력:** 요소 목록 `[{ text, type, bounds(x,y,w,h), clickable }]`
- **수용 기준:**
  - 코레일 앱의 각 화면에서 주요 버튼·텍스트가 좌표와 함께 수집된다.
  - 화면이 바뀌면 갱신된 목록을 다시 수집한다.
- **참고:** 순수 이미지 인식보다 안정적이고 빠름. 이미지 비전은 접근성 정보가 빈약한 화면에서만 보조로.

### F2. 목표 이해 / 입력
- **설명:** 사용자가 하고 싶은 일("기차표 예매하고 싶어")을 받아 목표로 설정한다.
- **MVP 방식:** 텍스트 입력 또는 사전 정의된 목표 버튼 선택(예: "기차표 예매하기")
- **수용 기준:** 목표가 설정되면 안내 루프(F6)가 시작된다.
- **확장:** 음성 입력은 F10에서.

### F3. 다음 단계 판단 (AI)
- **설명:** `현재 화면 요소 목록(F1) + 목표(F2) + 지금까지 진행 상태`를 AI에 전달해 **"지금 눌러야 할 딱 하나의 요소" + 안내 문구**를 결정한다.
- **출력:** `{ target_element, guidance_text, is_goal_reached }`
- **수용 기준:**
  - 코레일 홈에서 목표가 "예매"일 때 '승차권 예매'에 해당하는 요소를 지목한다.
  - 목표 화면 도달 시 `is_goal_reached=true`를 반환한다.
  - 한 번에 **하나의 액션만** 안내한다 (여러 단계 몰아서 X).
- **참고:** 화면 정보를 잘 정리해 넘기는 게 관건. 특정 앱에 하드코딩하지 않는다.

### F4. 캐릭터 안내원 표시·이동
- **설명:** 화면 오버레이(SYSTEM_ALERT_WINDOW) 위에 캐릭터를 띄우고, F3가 정한 요소 좌표로 **부드럽게 이동 → 가리키기 → 말풍선 표시**.
- **하위 동작:**
  - (a) 오버레이에 캐릭터 렌더링 및 목표 좌표로 이동
  - (b) 도착 후 가리키기 동작 + 말풍선/음성(F5) 동기화
- **수용 기준:**
  - 캐릭터가 이전 위치에서 다음 버튼 위치로 자연스럽게 이동한다.
  - "걸어가서 → 가리키며 → 말한다"가 한 세트로 맞물린다.
- **참고:** 리소스는 투명 배경 PNG 상태별 3~4장 + 걷기 2프레임으로 시작 (`04_기술아키텍처.md` 참조).

### F5. 음성 안내 (TTS)
- **설명:** 안내 문구를 음성으로 읽어준다("화면 아래 승차권 예매를 눌러주세요").
- **수용 기준:** 캐릭터 가리키기와 동시에 음성이 재생된다.
- **참고:** 안드로이드 기본 TTS 사용.

### F6. 화면 변화 감지 후 재판단 루프
- **설명:** 사용자가 버튼을 눌러 화면이 바뀌면 접근성 이벤트로 감지하고, F1→F3→F4를 다시 돌려 다음 단계를 안내한다.
- **수용 기준:**
  - 화면 전환 시 새 화면을 다시 읽고 안내가 갱신된다.
  - 목표 도달 전까지 이 루프가 끊기지 않는다.
- **참고:** 손이 가장 많이 가는 부분. 화면 전환 안정적 감지가 핵심. 상세 로직은 `03_화면워크플로우_및_코어로직.md`.

---

## 선택 기능 (시간 여유 시)

- **F7 캐릭터 표정·반응:** 대기 중 갸웃, 성공 시 박수/끄덕임 등. UX 온기↑
- **F8 겹침 회피 배치:** 캐릭터가 눌러야 할 버튼을 덮지 않도록 근처 빈 공간에서 가리키기
- **F9 복구 안내:** 사용자가 엉뚱한 화면에 가면 "괜찮아요, 뒤로 가기를 눌러주세요"
- **F10 음성 목표 입력(STT):** 타이핑 어려운 사용자용
- **F11 다중 앱:** 정부24·배달앱 등 (데모는 코레일만)
- **F12 접근성 모드:** 글자 확대·고대비
- **F13 캐릭터 선택:** 여러 캐릭터 중 선택

## 제외 기능 (이번 범위 아님)

- **X1 자동 실행:** 대신 눌러주기 → 자립 코칭 차별점과 정면 배치되므로 의도적 제외
- **X2 로그인 / X3 기록 저장 / X4 iOS / X5 다국어:** 데모 목적에 불필요
