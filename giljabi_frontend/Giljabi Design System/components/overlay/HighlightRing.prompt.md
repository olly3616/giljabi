Marks the exact control to tap, on top of someone else's app.

```jsx
<HighlightRing shape="pill" label="여기예요"><HostButton /></HighlightRing>
```

- Amber 4px ring + 3px black outer stroke, so it reads on light and dark hosts alike.
- Only ever one ring on screen at a time.
