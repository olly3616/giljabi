The home screen's goal picker row — icon chip plus a large label.

```jsx
<GoalCard icon="train-front" label="기차표 예매하기" caption="코레일에서 표를 끊어요" />
```

- Stack them vertically at 20px gaps; 4–6 per screen maximum.
- Selected = fill + thicker border + check icon (three signals, not just colour).
