# 개발 일정 (약 2주) — AI 디지털 약자 내비게이터

> **작업용 문서 세트 (5/5)**
> 기준: 과제 제출 9/20. 오늘부터 역산한 개략 일정 (팀 상황에 맞게 조정).
> 원칙: **핵심 루프가 도는 것**을 최우선. 캐릭터 꾸미기·선택 기능은 뒤로.

---

## 마일스톤 개요

| 단계 | 목표 | 성공 판정 |
|---|---|---|
| M1 | 파이프라인 뚫기 | 접근성으로 화면 읽고, 오버레이에 뭔가 띄운다 |
| M2 | 코어 루프 완성 | 목표 주면 AI가 다음 단계 판단→안내가 돈다 |
| M3 | 코레일 완주 + 캐릭터 | 코레일 예매를 캐릭터 안내로 끝까지 |
| M4 | 다듬기 + 발표 | 데모 안정화, 시연 대본, 발표 자료 |

---

## 상세 일정

### 1일차 — 셋업 & 리스크 선제거
- 안드로이드 프로젝트 생성(Kotlin), 접근성 서비스 + 오버레이 권한 요청 흐름 구현.
- **화면 읽기 최소 검증(F1):** 코레일 홈에서 버튼 텍스트·좌표가 로그로 찍히는지 확인.
- ⚠️ 여기가 최대 리스크. 1~2일차에 반드시 뚫어야 나머지가 성립.

### 2일차 — 눈과 손 연결 (M1)
- 오버레이에 테스트 뷰(점·박스)를 접근성이 준 좌표에 표시(F4-a 뼈대).
- 화면 전환 이벤트 수신 확인(F6 뼈대).
- **M1 판정:** "홈 화면의 '승차권 예매' 버튼 위에 마커가 뜬다"가 되면 통과.

### 3일차 — AI 판단 붙이기 (F3)
- (목표 + 요소목록 + 진행상태) → AI 호출 → JSON(target_text, guidance_text, is_goal_reached) 수신·파싱.
- target_text ↔ 화면 요소 매칭으로 좌표 확정.
- 홈 화면에서 "예매" 목표 주면 올바른 버튼을 지목하는지 검증.

### 4일차 — 코어 루프 완성 (M2)
- 안내 컨트롤러로 루프 연결: 화면읽기→AI→마커표시→화면변화대기→반복.
- 텍스트 말풍선(F4-b) + TTS 음성 안내(F5) 붙이기.
- **M2 판정:** 마커+말풍선+음성으로 코레일 2~3단계가 자동으로 이어지면 통과.

### 5일차 — 코레일 전체 흐름 잡기
- `03` 문서의 8단계를 실제 앱으로 검증, 실제 라벨·전환 반영.
- 각 화면에서 안정적으로 다음 단계가 안내되도록 예외 다듬기(안 누르면 재안내 등).

### 6~7일차 — 캐릭터 안내원 (F4 업그레이드)
- 마커를 **캐릭터**로 교체: 투명 PNG(idle/walk/point) + 좌표로 이동 애니메이션.
- 이동→가리키기→말풍선/음성 동기화.
- (여유) F8 겹침 회피 배치.
- **M3 판정:** 캐릭터가 코레일 예매를 예매 확인 화면까지 안내 완주하면 통과.

### 8~9일차 — 데모 안정화
- 시연 시나리오를 반복 테스트하며 끊기는 지점 제거.
- 실패 지점 폴백(재안내, 화면 못 읽을 때 대비).
- (여유) F7 캐릭터 반응, F12 접근성 모드 등 인상 요소.

### 10일차 — 예비/버퍼
- 밀린 작업 흡수. 새 기능 추가 금지, **안정화만**.

### 11~12일차 — 제출물 준비
- 시연 영상 녹화(현장 시연 실패 대비 필수).
- 발표 자료: 문제→해결→차별점(자립 코칭·실제 화면 인식·캐릭터)→데모→확장성(앱 무관 구조, iOS 로드맵).
- 제출 양식·설명 작성. **9/20 제출 마감 엄수.**

---

## 우선순위 규칙 (시간 부족 시)

1. **핵심 루프(F1~F6)가 도는 것**을 무슨 일이 있어도 지킨다.
2. 캐릭터는 "정확한 위치로 이동"만 되면 충분. 표정·선택·다중 캐릭터는 버린다.
3. 코레일 **1개 흐름 완주** > 여러 앱 어설프게.
4. 현장 시연이 불안하면 **녹화 영상**으로 대체 가능하게 미리 준비.

## 역할 분담 제안 (팀일 경우)
- **A:** 접근성 서비스 + 오버레이 (F1, F4, F6) — 가장 무겁고 앞단
- **B:** AI 판단 + 프롬프트 + 매칭 (F3, F2, F5)
- **C:** 캐릭터 리소스/애니메이션 + 발표 자료 + 디자인 시스템
- 1인이면: M1→M2→M3 순서 그대로, 캐릭터는 최소 스펙으로.
