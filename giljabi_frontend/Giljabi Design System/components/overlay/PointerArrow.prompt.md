Points from the character to the thing to tap.

```jsx
<PointerArrow direction="down" size={72} />
```

- Double-stroked: black casing under an amber core, so it survives any host colour.
- Pair with a HighlightRing; the arrow says "there", the ring says "exactly here".
