# 기술 아키텍처 — AI 디지털 약자 내비게이터

> **작업용 문서 세트 (4/5)**
> 안드로이드 기준. 접근성 서비스(눈) + 오버레이(손) + AI(두뇌)의 결합.

---

## 1. 전체 구조

```
┌─────────────────────────────────────────────┐
│                사용자 기기 (안드로이드)          │
│                                               │
│   [다른 앱: 코레일]                            │
│        ▲ 사용자가 실제로 조작                   │
│        │                                       │
│   ┌────┴───────────────┐   ┌──────────────┐   │
│   │ 접근성 서비스 (눈)   │   │ 오버레이 (손)  │   │
│   │ 화면 요소·좌표 수집  │   │ 캐릭터+말풍선   │   │
│   └────┬───────────────┘   └──────▲───────┘   │
│        │ 화면 정보                  │ 안내 표시   │
│        ▼                          │            │
│   ┌────────────────────────────┴──────────┐   │
│   │          안내 컨트롤러 (코어 루프)         │   │
│   │  화면정보 → AI 호출 → 결과로 캐릭터 제어    │   │
│   └────────────────┬─────────────────────┘   │
│                    │ (목표 + 화면요소)           │
└────────────────────┼─────────────────────────┘
                     ▼
              ┌──────────────┐
              │   AI (판단)    │  다음 눌러야 할 요소 + 안내문구
              └──────────────┘
```

## 2. 핵심 컴포넌트

### 2-1. 접근성 서비스 (AccessibilityService) — '눈'
- **역할:** 다른 앱 화면의 UI 요소를 읽는다.
- **핵심 권한:** `BIND_ACCESSIBILITY_SERVICE` (사용자가 설정에서 직접 켬)
- **하는 일:**
  - `onAccessibilityEvent`로 화면 전환·콘텐츠 변경 감지 (→ F6 재판단 트리거)
  - `rootInActiveWindow`에서 UI 트리를 순회하며 각 노드의 `text`, `className`, `bounds(getBoundsInScreen)`, `isClickable` 수집
- **출력 데이터 예시:**
  ```json
  [
    { "text": "승차권 예매", "type": "Button", "bounds": [40, 1600, 340, 1720], "clickable": true },
    { "text": "정기승차권", "type": "Button", "bounds": [360, 1600, 660, 1720], "clickable": true }
  ]
  ```
- **장점:** 이미지 인식보다 정확·빠름. 좌표가 정확해 캐릭터 이동에 바로 활용.

### 2-2. 화면 오버레이 (Overlay) — '손'
- **역할:** 다른 앱 위에 캐릭터·말풍선·화살표를 그린다.
- **핵심 권한:** `SYSTEM_ALERT_WINDOW` (사용자가 설정에서 직접 켬)
- **구현:** `WindowManager`에 `TYPE_APPLICATION_OVERLAY` 뷰를 추가. 캐릭터 뷰를 접근성 서비스가 준 좌표로 이동시킴.
- **주의:** 캐릭터가 눌러야 할 버튼을 덮지 않도록 배치 (F8). 오버레이 자체는 터치를 통과시켜 사용자가 아래 앱을 조작할 수 있게 설정(`FLAG_NOT_TOUCHABLE` 등 적절히).

### 2-3. 안내 컨트롤러 (코어 루프)
- **역할:** 접근성 서비스의 화면 정보를 받아 AI를 호출하고, 결과로 오버레이를 제어. `03_화면워크플로우_및_코어로직.md`의 B 루프를 담당.
- 상태 관리: 현재 목표, 진행 단계, 마지막 안내 등.

### 2-4. AI 판단 모듈 — '두뇌'
- **역할:** (목표 + 현재 화면 요소 목록 + 진행 상태) → 다음 액션 1개 + 안내 문구.
- **입력 프롬프트 구성(개념):**
  - 시스템: "너는 화면 요소 목록에서 목표 달성을 위해 사용자가 다음에 눌러야 할 요소 하나를 고른다. JSON만 출력."
  - 사용자: `{ goal, elements[], progress[] }`
- **출력(JSON 고정):**
  ```json
  { "target_text": "승차권 예매", "guidance_text": "화면 아래 승차권 예매를 눌러주세요", "is_goal_reached": false }
  ```
- **매칭:** AI가 고른 `target_text`를 화면 요소 목록과 대조해 좌표를 확정 → 캐릭터 이동.
- **참고:** 응답은 반드시 파싱 가능한 JSON으로. 실패 시 재시도/폴백 처리.

### 2-5. 음성 (TTS)
- 안드로이드 `TextToSpeech`로 `guidance_text` 재생. 캐릭터 가리키기와 동기화.

## 3. 데이터 흐름 (한 사이클)

```
화면 전환 감지(접근성 이벤트)
  → 화면 요소 목록 수집
  → AI 판단 호출 (목표+요소+진행)
  → JSON 결과 수신 (target_text, guidance_text, is_goal_reached)
  → target_text로 좌표 확정
  → 캐릭터 이동 + 가리키기 + 말풍선 + TTS
  → (사용자 조작) → 다시 처음으로
```

## 4. 캐릭터 리소스 스펙

- **금지:** JPEG (투명 배경 미지원 → 캐릭터 뒤 흰 네모)
- **권장 (MVP):** **투명 배경 PNG**
  - 상태별 이미지: `idle`(대기), `walk_1`,`walk_2`(걷기 2프레임), `point`(가리키기) — 최소 3~4장
  - 걷기 느낌은 walk 프레임을 빠르게 번갈아 표시(스프라이트)
  - 안드로이드 `ImageView`에 상태별로 교체
- **대안 (여유 시):** **Lottie(JSON)** — After Effects 벡터 애니메이션, 부드럽고 가벼움. Lottie 라이브러리 필요. LottieFiles에서 수급 가능하나 제작/선정 난이도 있음.
- **비추천:** GIF (투명 처리 지저분, 안드로이드 지원 약함), WebP는 PNG 대안으로 가능하나 데모 단계 이점 적음.
- **제작 시 주의:** 여러 상태에서 **같은 캐릭터로 일관성** 유지가 관건. 이미지 생성으로 뽑을 경우 특히 신경 쓸 것.

## 5. 기술 스택 제안

| 영역 | 선택 | 이유 |
|---|---|---|
| 플랫폼 | 안드로이드 네이티브 (Kotlin) | 접근성 서비스·오버레이가 네이티브 API에 직접 의존 |
| 화면 인식 | AccessibilityService | 좌표·텍스트 정확, 이미지 인식보다 안정 |
| 오버레이 | WindowManager + Overlay 권한 | 표준 방식 |
| AI | LLM API 호출 (JSON 출력 강제) | 판단 로직, 앱 무관 일반화 |
| 음성 | Android TextToSpeech | 기본 제공, 추가 비용 없음 |
| 캐릭터 | PNG 스프라이트 (→ 여유 시 Lottie) | 빠른 구현 |

## 6. 구현 순서상 주의

- **접근성 서비스 + 오버레이 권한 파이프라인을 가장 먼저** 세운다. 여기서 막히면 나머지가 안 돌아감.
- 화면 읽기(F1)와 오버레이 표시(F4-a)가 되면, 그 사이에 AI(F3)를 끼우는 순서가 안전.
- 재판단 루프(F6)는 화면 전환 감지가 관건이라 마지막에 안정화.
