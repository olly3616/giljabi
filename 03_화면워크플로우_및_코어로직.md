# 화면 워크플로우 & 코어 로직 — AI 디지털 약자 내비게이터

> **작업용 문서 세트 (3/5)**
> 두 부분: (A) 우리 앱 자체 화면 흐름 · (B) 코레일 예매를 안내하는 핵심 루프 로직

---

## A. 우리 앱 화면 흐름

우리 앱은 화면이 많지 않다. 대부분의 "화면"은 **다른 앱 위에 뜨는 오버레이**이기 때문이다.

```
[온보딩/권한] → [홈: 목표 선택] → [안내 오버레이(다른 앱 위)] → [완료]
```

### A-1. 온보딩 / 권한 요청
- 접근성 서비스 권한 + 화면 오버레이 권한을 안내에 따라 켜도록 유도.
- **주의:** 이 두 권한은 사용자가 시스템 설정에서 직접 켜야 함(자동 불가). 큰 글씨 + 그림으로 단계 안내.
- 데모 팁: 시연 기기에는 권한을 미리 켜두고, 온보딩은 발표 슬라이드로만 설명해 시간 절약.

### A-2. 홈 — 목표 선택
- 사용자가 무엇을 도와줄지 고르는 화면.
- MVP: 큰 버튼으로 사전 정의된 목표 제시 (예: **"기차표 예매하기"**) + 자유 텍스트 입력칸.
- 목표 선택 → 캐릭터가 등장하고 안내 루프(B) 시작 → 자동으로 코레일 앱을 열거나, 사용자가 열도록 안내.

### A-3. 안내 오버레이
- 이후 모든 안내는 다른 앱(코레일) 위에 뜨는 캐릭터+말풍선으로 진행. (B의 루프)

### A-4. 완료
- 목표 도달 시 캐릭터가 축하 + "다 하셨어요!" 안내. 홈으로 복귀.

---

## B. 코어 안내 루프 로직

이 루프가 이 서비스의 심장이다. **화면을 읽고 → 다음 한 단계를 정하고 → 안내하고 → 사용자의 행동으로 화면이 바뀌면 다시 반복**한다.

### B-1. 의사코드

```
목표 = 사용자가_선택한_목표()          // 예: "기차표 예매"
진행상태 = []                          // 지금까지 거친 단계 기록

반복:
    요소목록 = 접근성서비스.현재화면_읽기()      // F1: [{text, type, bounds, clickable}, ...]

    결정 = AI.다음단계_판단(목표, 요소목록, 진행상태)   // F3
    // 결정 = { target_element, guidance_text, is_goal_reached }

    만약 결정.is_goal_reached 이면:
        캐릭터.축하동작()
        음성.말하기("다 하셨어요!")
        루프 종료

    좌표 = 결정.target_element.bounds
    캐릭터.이동(좌표)                    // F4-a
    캐릭터.가리키기()                    // F4-b
    말풍선.표시(결정.guidance_text)       // F4-b
    음성.말하기(결정.guidance_text)       // F5

    진행상태.추가(결정.target_element)

    화면변화_대기()                      // F6: 접근성 이벤트로 화면 전환 감지
    // 화면이 바뀌면 다시 반복 위로
```

### B-2. 각 단계 상세

| 단계 | 무엇을 | 어떻게 | 관련 기능 |
|---|---|---|---|
| 1. 화면 읽기 | 현재 화면의 버튼·텍스트·좌표 수집 | 접근성 서비스가 UI 트리를 순회 | F1 |
| 2. 판단 | 목표 대비 "다음 한 걸음" 결정 | AI에 (목표+요소목록+진행상태) 전달 | F3 |
| 3. 종료 체크 | 목표 화면 도달 여부 | AI가 `is_goal_reached` 반환 | F3 |
| 4. 안내 | 캐릭터 이동+가리키기+말풍선+음성 | 오버레이 렌더링 + TTS | F4, F5 |
| 5. 대기 | 사용자 행동으로 화면 전환 감지 | 접근성 이벤트 리스닝 | F6 |
| 6. 반복 | 1번으로 | — | F6 |

### B-3. 예외/엣지 케이스 (데모 최소 대응)

- **사용자가 한동안 안 누름:** 일정 시간 뒤 같은 안내를 음성으로 한 번 더 반복.
- **엉뚱한 화면으로 이동:** (선택 F9) "괜찮아요, 뒤로 가기를 눌러주세요"로 복귀 유도. 데모에선 정해진 경로만 시연해 회피 가능.
- **목표 요소를 화면에서 못 찾음:** 스크롤이 필요한 경우 "화면을 위로 올려주세요" 안내. (여유 시 구현)

---

## C. 코레일 예매 데모 시나리오 (단계별)

> ⚠️ **중요:** 아래 화면·버튼 명칭은 예상 흐름이다. **실제 최신 코레일 앱으로 반드시 검증**하고 실제 라벨에 맞춰야 한다.
> 단, 우리 구조는 라벨을 하드코딩하지 않고 **화면에서 읽은 실제 텍스트로 판단**하므로, 앱이 조금 달라도 동작하도록 설계한다.

| # | 화면 | 사용자 목표 진행 | 캐릭터 안내(예상) |
|---|---|---|---|
| 1 | 코레일 홈 | 예매 시작 | "화면 아래 **승차권 예매**를 눌러주세요" |
| 2 | 출발/도착역 선택 | 출발역 지정 | "**출발역**을 눌러 도시를 골라주세요" |
| 3 | 출발/도착역 선택 | 도착역 지정 | "이번엔 **도착역**을 골라주세요" |
| 4 | 날짜/시간 선택 | 일정 지정 | "**날짜**를 눌러 원하는 날을 골라주세요" |
| 5 | 조회 | 열차 검색 | "**조회하기**를 눌러주세요" |
| 6 | 열차 목록 | 열차 선택 | "원하는 시간의 열차를 눌러주세요" |
| 7 | 좌석/운임 | 좌석 선택 | "**좌석 선택**을 눌러주세요" |
| 8 | 예매 확인 | 목표 도달 지점 | "예매 직전까지 왔어요! 확인해주세요" → 완료 |

**데모 종료 지점 정의:** 결제 정보 입력 직전(예매 확인 화면 도달)을 목표 도달로 잡는 것을 권장. 실제 결제까지 시연할지는 팀이 결정하되, 개인정보/결제 이슈를 피하려면 확인 화면까지가 안전.

### 이 시나리오로 확정해야 할 것
1. 실제 코레일 앱을 켜서 위 8단계의 **진짜 버튼 라벨과 화면 전환**을 캡처·기록.
2. 각 화면에서 접근성 서비스가 목표 요소를 실제로 읽어오는지 확인.
3. 캐릭터가 이동할 좌표가 각 화면에서 안정적으로 잡히는지 확인.
